# React Native (Expo)

Start the dev server from the `frontend/` directory:

```bash
npm start
```

Then, with the Metro bundler running:

- Press `i` to open in the iOS simulator
- Press `a` to open in the Android emulator

Node.js is needed for this